# PaintBoard — Backlog

> 最后更新：2026-03-25（v1.0.0 发布）

---

## ⚠️ 待办：重新梳理后续开发规划

> 下次开始前，先整体梳理后续功能优先级、版本节奏和技术方案，再启动开发。

---

## v1.1 待办

### F11 导入图片底图
- 从相册/文件管理器选择图片，插入当前页作为背景
- 背景图不可书写，只作参考底图
- 协同时背景图需同步到所有端
- **状态**：⏳ 未开始

### 双向箭头图形
- 图形工具新增双向箭头类型
- 复用现有 `ShapeRenderer` + `ShapePanel` 扩展
- **状态**：⏳ 未开始

### 主持人锁定模式（协同）
- HOST 可开启"主持人控制"：开启后只有 HOST 可翻页，参与者视角跟随 HOST
- 需新增协同消息类型（`HOST_LOCK` / `HOST_UNLOCK`）
- **状态**：⏳ 未开始

---

## v1.5 待办

### F12 手写便利贴
- 有背景色的可移动卡片容器，内部手写笔画
- 引入"可移动对象层"架构（`domain/model/` 需扩展 `StickyNote` 模型）
- 协同同步包括位置、内容、颜色变更
- v1.0 架构已预留对象层接口
- **状态**：⏳ 未开始

---

## 优化项 / 技术债

| 项目 | 说明 | 优先级 |
|------|------|--------|
| ~~**书写流畅度（方案 B）**~~ | ~~双层混合渲染方案~~（已验收，关闭） | ~~高~~ |
| 协同光标显示 | `CURSOR_MOVE` 消息已预留，未实现不同设备实时彩色光标 | 低 |
| 扫码分享 PDF 导出 | 当前只支持 PNG 全部页，PRD 要求支持 PDF 一键下载 | 中 |
| 大屏掌心擦除 | PRD F02 要求基于触点面积区分书写/擦除（大屏模式），当前未实现 | 中 |
| PAD 模式触控笔自动擦除 | PRD F02 要求触控笔写、手指擦，当前未实现自动识别 | 中 |

---

## Bug 跟踪

> 暂无已知 Bug
